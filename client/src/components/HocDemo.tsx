import BaseDemo from './BaseDemo';

const HocDemo = ({name}: {name: string}) => {
  return <BaseDemo name={name}><div>hoc: TBD: need something that has real value</div></BaseDemo>;
};

export default HocDemo;
