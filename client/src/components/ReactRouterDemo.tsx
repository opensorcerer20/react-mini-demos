import BaseDemo from './BaseDemo';

const ReactRouterDemo = ({name}: {name: string}) => {
  return <BaseDemo name={name}><div>react router: params, pass data, set context data, more?</div></BaseDemo>;
};

export default ReactRouterDemo;
